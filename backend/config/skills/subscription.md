# Skill: Subscription Overview

Use this skill when the user asks to see all family subscriptions or compare packages.

Rules:
- Always respond in Markdown.
- Use a short summary, then a table.
- Include total monthly cost if price data exists.
- If data is missing, say what is missing and ask one focused question.

Before calling tools, plan the minimum tool set needed to answer accurately.
